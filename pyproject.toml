[project]
name = "dhis2-health-pipeline"
version = "0.1.0"
description = "psi disc dhis2 health data pipeline challenge"
readme = "README.md"
requires-python = ">=3.10,<3.12"
dependencies = [
    "pyspark==3.4.1",
    "faker>=24.0.0",
    "numpy>=1.24.0",
    "pytest>=9.0.3",
]

[tool.uv]
managed = true
